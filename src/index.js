'use strict'

/**
 * Module
 * @ignore
 */
const Registry = require('./Registry')
const Action = require('./Action')
const Selector = require('./Selector')

/**
 * Exports
 * @ignore
 */
module.exports = {
  Registry,
  Action,
  Selector
}
